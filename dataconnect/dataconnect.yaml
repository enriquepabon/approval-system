specVersion: "v1beta"
serviceId: "approval-system"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "approval-system-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
